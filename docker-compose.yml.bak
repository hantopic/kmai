version: "3.8"

services:
  kmai-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: kmai-backend
    restart: always
    ports:
      - "127.0.0.1:8010:8000"
